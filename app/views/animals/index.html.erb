<h1>Liste des Animaux</h1>
<ul>
  <% @animals.each do |animal| %>
    <li> <%= animal.id %> <%= animal.name %> - <%= animal.species %> - Propriétaire ID : <%= animal.owner_id %></li>
  <% end %>
</ul>
